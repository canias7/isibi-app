{
  "name": "isibi-game-builder",
  "private": true,
  "type": "module",
  "description": "Deps pre-baked into the game build-service container image so each per-game build is just `vite build` (fast). The generator emits kaplay game source that uses ONLY these packages. Mirrors ../builder (the React app builder), engine swapped for kaplay.",
  "dependencies": {
    "kaplay": "^3001.0.0"
  },
  "devDependencies": {
    "vite": "^5.4.0",
    "playwright-core": "^1.47.0"
  }
}
