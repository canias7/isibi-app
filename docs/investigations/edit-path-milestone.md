# Edit-path review milestone

Base: main `38fe281d`. Working branch: `codex/edit-path-milestone`.
The escalation and fallback-display corrections are closed. Their existing
controls remain in scope; deployment checks are not repeated.

Authorized: bounded product/harness fixes, local fixture scenarios, focused
tests, reviewable pushes and required final CI. No merge, deployment, paid
request, customer-data mutation or demo repair. Translation, video hosting and
model-written replies remain parked.

## Coverage map in progress

The actual router exposes data, text, look, page, rules, picture, logo, nav and
rename. The look picker has ten own fields (css, theme, brand, description,
wordmark, favicon, lang, langs, behavior, qr); dispatches purpose/components/
shape/three/tsx to page, images to picture, action to nav, backend to rules and
slug to rename; pages uses verbs; kind requests reconstruction. Translation
fields are recorded only, not exercised in this milestone.

Existing strong route fixtures inspect writer requests, compiler payloads,
stored source and browser reply: edit-page-context, edit-page-once,
edit-page-protect, edit-page-keep, edit-failure and edit-path. Queue tests mix
executed behavior and source-order assertions; these must not be conflated.

Known evidence limits: supplied model output does not establish real model
compliance; a compiler stub establishes its payload, not successful compilation
or rendering. Existing preservation tests explicitly show plain-text section
loss outside the inventory and dependence on a model's removal judgment.

## Work log

- Started from clean main; derived operation map from module exports.
- Reviewing attachment/context wiring, targeted component preservation,
  tweak-to-writer fallback, mixed outcomes, backend state, publication/billing,
  queue recovery and browser continuation.
- Remaining checklist investigations: queue handoff marker, terminal display
  exceptions and preview update ordering. No product changes yet.

## Final evidence and decisions

Pending. Keep unsupported capability, reproduced defect and missing live
observation separate. Record exact tests and CI on the final candidate.

### Progress checkpoint

Reproduced and corrected on this branch: queued hop marker lost (including
reload), queued refusal redraw exceptions, preview invalidation skipped by a
synchronous scheduling error, terminal job scheduling error, and missing-source
edit reconstruction despite unreadable config. Edit source loading now preserves
failed editable recovery and uses the existing strict reader. No pricing or
permission change; no new writer or reconstruction capability.

180 selected real-route/preservation/backend cases pass. Browser group adds
regressions and keeps closed controls. Two CRLF-dependent reads in existing
fixtures were normalized; remaining old source-scan/path assumptions fail on
Windows and will be distinguished from required Linux CI. Stored component
output is additionally rendered with real React: requested words appear, neighbor
markup is identical. This is supplied TSX, not model compliance or live rendering.

Remaining work: finish operation coverage and limitations, validate billing/
publication and input seams, evaluate a bounded rendering interaction where
needed, run required CI on the final candidate and publish the review report.
