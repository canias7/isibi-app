// GENERATED from isibi.schema.json — do not edit by hand. Change the schema and this follows.
//
// This is the TEMPLATE default, used when an app declares no tables. The build regenerates this file from the
// app's own isibi.schema.json, so `useResource('bookings')` returns a typed row with no annotation at the call
// site and a column typo is a compile error instead of `undefined` on screen.

// This app declares no tables, so every useResource row stays `any`.
export interface Tables {}
