[
  {
    "label": "CONTROL: the correction-billing comment changes nothing",
    "control": true,
    "files": [
      "worker.js"
    ],
    "from": "                // THE CORRECTION IS NOT BILLED, and that is a decision rather",
    "to": "                // THE CORRECTION IS NOT BILLED (control), and that is a decision rather"
  },
  {
    "label": "R-2: a correction that still misses stops without putting the design back",
    "files": [
      "worker.js"
    ],
    "from": "                if (eJob && !finalPub.ok && finalPub.error === \"dead-css\") {\n                  const kept = !(await restoreEditConfig());",
    "to": "                if (eJob && !finalPub.ok && finalPub.error === \"dead-css\") {\n                  const kept = false;"
  },
  {
    "label": "R-3: a failed publish after the round does not put the design back",
    "files": [
      "worker.js"
    ],
    "from": "the change. `compileMsg` now carries `kept` to every arm.\n                const kept = !(await restoreEditConfig());",
    "to": "the change. `compileMsg` now carries `kept` to every arm.\n                const kept = false;"
  }
]
