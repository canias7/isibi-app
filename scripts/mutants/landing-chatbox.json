[
  { "label": "CONTROL: the landing's placement note changes nothing", "control": true,
    "files": ["public/styles.css"],
    "from": "What stays the landing's own is only where it sits:",
    "to": "What stays the landing's own (control) is only where it sits:" },
  { "label": "CONTROL: the landing box's markup note changes nothing", "control": true,
    "files": ["public/index.html"],
    "from": "No Attach and no Builder",
    "to": "No Attach (control) and no Builder" },
  { "label": "the landing's box goes back to a class of its own — two classes, two looks",
    "files": ["public/index.html"],
    "from": "<div class=\"st-new\" id=\"crtChatbox\">",
    "to": "<div class=\"crt-chatbox\" id=\"crtChatbox\">" },
  { "label": "the landing's send button goes back to a class of its own",
    "files": ["public/index.html"],
    "from": "<button class=\"st-gen\" id=\"crtLandSend\"",
    "to": "<button class=\"crt-chatbox-send\" id=\"crtLandSend\"" },
  { "label": "the landing's send button takes the builder's id — renderSites would bind the landing's copy",
    "files": ["public/index.html"],
    "from": "id=\"crtLandSend\"",
    "to": "id=\"stGen\"" },
  { "label": "the field rule is scoped by the builder's PAGE again, so it can only reach the builder's copy",
    "files": ["public/styles.css"],
    "from": ".st-new .st-in { width: 100%;",
    "to": ".st-hero .st-in { width: 100%;" },
  { "label": "a landing rule restyles the builder's box (a radius of its own)",
    "files": ["public/styles.css"],
    "from": ".crt-inbox .st-new{width:min(720px,100%);margin-top:0;font-size:1rem;line-height:normal}",
    "to": ".crt-inbox .st-new{width:min(720px,100%);margin-top:0;font-size:1rem;line-height:normal;border-radius:14px}" },
  { "label": "the landing's text context is left at 17px / 1.55 — the same box, 2.34px taller (only a browser sees an inherited value)",
    "files": ["public/styles.css"],
    "from": ".crt-inbox .st-new{width:min(720px,100%);margin-top:0;font-size:1rem;line-height:normal}",
    "to": ".crt-inbox .st-new{width:min(720px,100%);margin-top:0}" },
  { "label": "the landing's box is placed narrower than the builder's (620 against 720)",
    "files": ["public/styles.css"],
    "from": ".crt-inbox .st-new{width:min(720px,100%);margin-top:0;font-size:1rem;line-height:normal}",
    "to": ".crt-inbox .st-new{width:min(620px,100%);margin-top:0;font-size:1rem;line-height:normal}" },
  { "label": "the box says something other than the owner's word",
    "files": ["public/chat.js"],
    "from": ": live ? 'hey' :",
    "to": ": live ? 'hi' :" },
  { "label": "the markup writes a placeholder of its own — a second writer of paintCrt's line",
    "files": ["public/index.html"],
    "from": "placeholder=\"\"",
    "to": "placeholder=\"hey\"" }
]
