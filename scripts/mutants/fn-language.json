[
  {
    "label": "CONTROL: a comment in site-rls changes nothing",
    "control": true,
    "files": [
      "site-rls.mjs"
    ],
    "from": "export const FN_LANGUAGES = Object.freeze([\"sql\", \"plpgsql\"]);",
    "to": "// a comment\nexport const FN_LANGUAGES = Object.freeze([\"sql\", \"plpgsql\"]);"
  },
  {
    "label": "CONTROL: a comment in site-add changes nothing",
    "control": true,
    "files": [
      "builder/site-add.mjs"
    ],
    "from": "        if (language !== undefined && !FN_LANGUAGES.includes(language)) return { ok: false, why: \"bad-language\" };",
    "to": "        // a comment\n        if (language !== undefined && !FN_LANGUAGES.includes(language)) return { ok: false, why: \"bad-language\" };"
  },
  {
    "label": "the tool stops offering a language at all",
    "files": [
      "builder/site-table.mjs"
    ],
    "from": "    language: { type: \"string\", enum: FN_LANGUAGES.slice(), description:",
    "to": "    languageXX: { type: \"string\", enum: FN_LANGUAGES.slice(), description:"
  },
  {
    "label": "the tool's enum is written out instead of derived from the engine's list",
    "files": [
      "builder/site-table.mjs"
    ],
    "from": "enum: FN_LANGUAGES.slice(), description:",
    "to": "enum: [\"sql\", \"plpgsql\", \"plpython3u\"], description:"
  },
  {
    "label": "the platform's own frozen list goes onto the wire",
    "files": [
      "builder/site-table.mjs"
    ],
    "from": "enum: FN_LANGUAGES.slice(), description:",
    "to": "enum: FN_LANGUAGES, description:"
  },
  {
    "label": "the cleaner drops the language again (the reported gap)",
    "files": [
      "builder/site-add.mjs"
    ],
    "from": "...(language === undefined ? {} : { language })",
    "to": "...(true ? {} : { language })"
  },
  {
    "label": "the cleaner converts an unknown language instead of refusing it",
    "files": [
      "builder/site-add.mjs"
    ],
    "from": "        if (language !== undefined && !FN_LANGUAGES.includes(language)) return { ok: false, why: \"bad-language\" };",
    "to": "        if (false) return { ok: false, why: \"bad-language\" };"
  },
  {
    "label": "the cleaner refuses a language it should accept, by not folding case",
    "files": [
      "builder/site-add.mjs"
    ],
    "from": "          : String(v.language).trim().toLowerCase();",
    "to": "          : String(v.language).trim();"
  },
  {
    "label": "an empty language is read as a language rather than as absence",
    "files": [
      "builder/site-add.mjs"
    ],
    "from": "        const language = v.language === undefined || v.language === null || v.language === \"\"",
    "to": "        const language = v.language === undefined || v.language === null"
  },
  {
    "label": "a function that said nothing about its language gains the key anyway",
    "files": [
      "builder/site-add.mjs"
    ],
    "from": "...(language === undefined ? {} : { language })",
    "to": "language"
  },
  {
    "label": "the refusal loses its own sentence and falls to the generic one",
    "files": [
      "builder/site-add.mjs"
    ],
    "from": "    case \"bad-language\": return \"That one asked to be written in a database language this platform doesn't run",
    "to": "    case \"bad-language-XX\": return \"That one asked to be written in a database language this platform doesn't run"
  },
  {
    "label": "the FOLD drops the language again — the second rebuild (the real defect this round found)",
    "files": [
      "builder/site-add.mjs"
    ],
    "from": "      const { exists, ...fn } = v;\n      functions.push(fn);",
    "to": "      functions.push({ name: v.name, args: v.args, returns: v.returns, body: v.body, internal: v.internal === true });"
  },
  {
    "label": "the CLEANER stops forcing `internal` to a real boolean (the wall the fold no longer duplicates)",
    "files": [
      "builder/site-add.mjs"
    ],
    "from": "internal: v.internal === true, ...(language === undefined ? {} : { language })",
    "to": "internal: v.internal, ...(language === undefined ? {} : { language })"
  },
  {
    "label": "the fold carries the cleaner's own bookkeeping onto the wire",
    "files": [
      "builder/site-add.mjs"
    ],
    "from": "      const { exists, ...fn } = v;\n      functions.push(fn);",
    "to": "      functions.push({ ...v });"
  },
  {
    "label": "the normaliser stops reading the language",
    "files": [
      "site-schema.mjs"
    ],
    "from": "    const lang = fnLanguage(f.language);",
    "to": "    const lang = fnLanguage(undefined);"
  },
  {
    "label": "the normaliser forks its own copy of the language rule",
    "files": [
      "site-schema.mjs"
    ],
    "from": "    const lang = fnLanguage(f.language);",
    "to": "    const lang = String(f.language || \"sql\").toLowerCase() === \"plpgsql\" ? \"plpgsql\" : \"plpgsql\";"
  },
  {
    "label": "the emitter forks its own copy of the language rule",
    "files": [
      "site-rls.mjs"
    ],
    "from": "    \" LANGUAGE \" + fnLanguage(f.language) +",
    "to": "    \" LANGUAGE sql\" + (f.language ? \"\" : \"\") +"
  },
  {
    "label": "an unknown language falls to plpgsql instead of the default",
    "files": [
      "site-rls.mjs"
    ],
    "from": "  return FN_LANGUAGES.includes(s) ? s : FN_LANGUAGES[0];",
    "to": "  return FN_LANGUAGES.includes(s) ? s : \"plpgsql\";"
  },
  {
    "label": "the reader stops folding case, so a stored `PLpgSQL` becomes sql",
    "files": [
      "site-rls.mjs"
    ],
    "from": "  const s = String(v == null ? \"\" : v).trim().toLowerCase();",
    "to": "  const s = String(v == null ? \"\" : v).trim();"
  },
  {
    "label": "a third language joins the engine's list",
    "files": [
      "site-rls.mjs"
    ],
    "from": "export const FN_LANGUAGES = Object.freeze([\"sql\", \"plpgsql\"]);",
    "to": "export const FN_LANGUAGES = Object.freeze([\"sql\", \"plpgsql\", \"plpython3u\"]);"
  },
  {
    "label": "the stored declaration loses the language",
    "files": [
      "site-schema.mjs"
    ],
    "from": "internal: !!f.internal, language: fnLanguage(f.language) });",
    "to": "internal: !!f.internal });"
  },
  {
    "label": "the stored declaration records a language that is not the one applied",
    "files": [
      "site-schema.mjs"
    ],
    "from": "internal: !!f.internal, language: fnLanguage(f.language) });",
    "to": "internal: !!f.internal, language: \"sql\" });"
  },
  {
    "label": "the audit does not know the tool offers a language",
    "files": [
      "site-schema.mjs"
    ],
    "from": "  function: new Set([\"name\", \"args\", \"returns\", \"body\", \"internal\", \"language\"]),",
    "to": "  function: new Set([\"name\", \"args\", \"returns\", \"body\", \"internal\"]),"
  },
  {
    "label": "the next designer is never told a function is not ordinary SQL",
    "files": [
      "worker.js"
    ],
    "from": "              fnLangs: Object.fromEntries(((spec && spec.functions) || [])",
    "to": "              fnLangsXX: Object.fromEntries(((spec && spec.functions) || [])"
  },
  {
    "label": "every function is marked with its language, including the ordinary ones",
    "files": [
      "worker.js"
    ],
    "from": "                .filter((f) => f && f.name && f.language && f.language !== \"sql\")",
    "to": "                .filter((f) => f && f.name)"
  },
  {
    "label": "the note stops marking the language it was handed",
    "files": [
      "builder/site-add.mjs"
    ],
    "from": "  if (fns.length) lines.push(\"Its database functions are: \" + fns.map(fnLang).join(\", \") + \".\");",
    "to": "  if (fns.length) lines.push(\"Its database functions are: \" + fns.join(\", \") + \".\");"
  },
  {
    "label": "the note's marker cannot match a name this change is adding",
    "files": [
      "builder/site-add.mjs"
    ],
    "from": "    const raw = String(n).replace(/\\s*\\(being added by this same change\\)$/, \"\");",
    "to": "    const raw = String(n);"
  }
]
