{
  "name": "isibi-app",
  "private": true,
  "description": "Worker dependencies (bundled by wrangler at deploy). The app itself is plain static files in public/.",
  "dependencies": {
    "@cf-wasm/photon": "^0.3.6",
    "@cloudflare/containers": "^0.3.7",
    "@neondatabase/serverless": "^1.0.2",
    "qrcode-generator": "^2.0.4"
  },
  "scripts": {
    "test": "node --test \"test/*.test.mjs\""
  },
  "devDependencies": {
    "typescript": "^5.9.3"
  }
}
